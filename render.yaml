services:
  - type: web
    name: GIN_backend
    env: python
    buildCommand: |
      pip install -r requirements.txt
      python manage.py migrate
      python manage.py collectstatic --noinput
    startCommand: gunicorn GIN_backend.wsgi:application
